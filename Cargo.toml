[package]
name = "multilayer-perceptron"
version = "0.1.0"
edition = "2024"

[lib]
name = "multilayer_perceptron"
path = "src/lib.rs"

[dependencies]
rand = "0.9.0"
chrono = "0.4"
serde = { version = "1", features = ["derive"] }
serde_json = "1.0.149"
