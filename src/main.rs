mod linear_algebra;
mod testing;

use crate::testing::generation::test;

fn main() {
    test();

    // let matrix = Matrix.random((3, 2));
    // println!("{:?}", matrix);
    // println!("{:?}", matrix.transpose());
}
